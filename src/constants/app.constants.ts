export const START_SERVER = (port: string | number) => {
  console.log(
    '----------------------------------------------------------------',
  );
  console.log(
    `🚀 Server started on port: ${port} 🔥🔥🔥 All code in project is 👍`,
  );
  console.log(
    '----------------------------------------------------------------',
  );
};
